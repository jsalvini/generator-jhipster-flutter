import 'package:flutter/material.dart';
import 'package:font_awesome_flutter/font_awesome_flutter.dart';

class <%= camelizedUpperFirstBaseName %>Routes {
  static const login = '/login';
  static const splash = '/splash';
  static const home = '/home';
  static const settings = '/settings';
  static const user = '/user';
  // jhipster-merlin-needle-routes-entry-add

  static const double iconSize = 24;
  static const Map<String, dynamic> screens = {
    'Inicio': {
      'routeName': '/home',
      'icon': Icon(
        Icons.home,
        size: iconSize,
      ),
    },
    'Perfil': {
      'routeName': '/settings',
      'icon': FaIcon(
        FontAwesomeIcons.gear,
        size: iconSize,
      ),
    },
  };

  static const Map<String, dynamic> screensEntities = {
    // jhipster-merlin-needle-screens-entry-add
  };
}